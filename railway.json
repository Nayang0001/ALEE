{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "python3 -m http.server ${PORT:-8080} --bind 0.0.0.0 --directory .",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}